import SwiftUI

/// Shared accent (matches app ~ #F59E0B).
enum PPWatchComplicationTheme {
    static let accent = Color(red: 0.961, green: 0.620, blue: 0.071)
}

enum ComplicationBrand {
    static let title = "hYYa"
}
