import Foundation

enum WatchPrayerWidgetConstants {
    static let appGroupId = "group.com.hyya.prayerpal"
    static let snapshotKey = "watchPrayerWidget.snapshot.v1"
    /// Must match `WidgetCenter.reloadTimelines(ofKind:)` from the watch app.
    static let widgetKind = "PrayerPalWatchNextPrayer"
}
